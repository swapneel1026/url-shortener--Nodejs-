<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
<h1>URl Shortener</h1>
<% if (locals.id) { %>
    <h2>Shortened Url: http://localhost:7000/urlshortener/<%=id %></h2> 
<% } %>

<form action="/url" method="POST">
    <label for="url">Url:</label>
    <input type="text" name="url" placeholder="Enter the URL" required>
    <button type="submit">Submit</button>
</form>
<h2>All Linkss:</h2>
<% if (locals.urls) { %>
    <ul>
  <% urls.forEach(url => { %>
   <li>http://localhost:7000/urlshortener/<%= url.shortId %></li>
  <% }) %>
 </ul>
<% } %>

    
</body>
</html>